import { createGlobalStyle } from 'styled-components';

const mainpageglobalstyle = createGlobalStyle`
  html, body {
    margin: 0;
    padding: 0;
    font-family: 'Inter', sans-serif;
  }
`;

export default mainpageglobalstyle;
